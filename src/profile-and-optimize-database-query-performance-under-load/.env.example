# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=query_optimization_db

# Database Connection Pool Tuning (adjust based on load test results)
DB_POOL_MIN=50
DB_POOL_MAX=100

# Application Configuration
NODE_ENV=development
PORT=3000

# Load Testing
BASE_URL=http://localhost:3000
