// This file is intentionally left as a re-export shim.
// The canonical implementation is in database.config.ts.
// ts-jest will resolve the .ts file when running tests.
module.exports = require('./database.config.ts');
